package app;

public class Corrector {
    public String handleData(String[] strs) {
        stringBuilder = new StringBuilder();
        int count = 0;
        for (String str : strs) {
            if (str.("b")) {
                str = str.replac("b", "o");
            }
            count;
            stringBuilder.append(count).append(") ").append(str).append("\\n");
        }
        return stringBuilber.toString();
    }
}
